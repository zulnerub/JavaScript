{{#each monkeys}}
    <div class="monkey">
        <h2>{{name}}</h2>
        <img src="{{image}}">
        <button id="{{id}}" onclick="toggle(this.id)">Info</button>
        <p style="display: none" id="info_{{id}}">{{info}}
        </p>
    </div>
{{/each}}